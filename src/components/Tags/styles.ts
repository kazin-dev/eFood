import styled from 'styled-components'
import { cores } from '../../styles'

export const Cate =
  styled.span <
  Omit(teste) >
  `
  width: 61px;
  height: 26px;
  background-color: ${cores.rosa};
  color: ${cores.branco};
`
