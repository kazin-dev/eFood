import styled from 'styled-components'
import { cores } from '../../styles'

export const BotaoPrimeiro = styled.button`
  width: 82px;
  height: 24px;
  background-color: ${cores.rosa};
  color: ${cores.branco};
  border: none;
  cursor: pointer;
`
